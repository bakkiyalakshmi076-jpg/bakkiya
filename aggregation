create database e_commerce;

use e_commerce;

create table shopping
(product_id int,
product_name varchar(10),
product_price int);

desc shopping;

insert into shopping(product_id,product_name,product_price)
 values (1,'shirt',600),(2,'phant',700),(3,'tops',800),(4,'salwar',900),
(7,'t-shirt',900),(8,'baggy',400),(9,'mesh top',1000);

select*from shopping;

SELECT product_name, price 
FROM shopping 
WHERE price = (SELECT MAX(price) FROM shopping);

SELECT product_name, price 
FROM shopping 
WHERE price = (SELECT MIN(price) FROM shopping);
